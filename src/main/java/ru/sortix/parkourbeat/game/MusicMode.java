package ru.sortix.parkourbeat.game;

public enum MusicMode {
    DISABLED,
    PIECES,
    FULL_TRACK,
    /**
     * Трек нарезан по чекпоинтам уровня и играется кусками: каждый кусок от своего
     * чекпоинта до следующего. При проигрыше игрок возвращается на чекпоинт, и заново
     * запускается ровно тот кусок, который на этом чекпоинте начинается.
     * <p>
     * Это НЕ режим посекундной синхронизации ({@link #PIECES}) — тот режим давно не
     * поддерживается и здесь ни при чём.
     */
    CHECKPOINTS
}
